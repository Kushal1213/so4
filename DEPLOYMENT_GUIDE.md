# Deployment Guide (Legacy)

This file has been superseded by **[DEPLOYMENT.md](DEPLOYMENT.md)**.

Use the new guide for:

- Docker deployment (`docker compose up --build`)
- Render Blueprint (one-click cloud deploy)
- Vercel frontend deployment
- GitHub Actions CI/CD
- Environment variables and troubleshooting

Quick validation before deploy:

```bash
powershell -ExecutionPolicy Bypass -File scripts/deploy-prep.ps1
```
